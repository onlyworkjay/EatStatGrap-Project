import styles from "./ReviewListPage.module.css";

const ReviewListPage = () => {
    return (
        <div>
            <h1 className={styles.pageTitle}>후기</h1>
        </div>
    );
};

export default ReviewListPage;